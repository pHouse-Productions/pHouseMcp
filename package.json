{
  "name": "phousemcp",
  "version": "1.0.0",
  "description": "Modular MCP servers with shared packages",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "servers/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
